# 6️⃣ Deploy dell’app

Esegui il primo deploy con Kamal:

```bash
kamal deploy production
```

Kamal costruirà le immagini Docker, avvierà PostgreSQL, Redis, Rails e Traefik, e Traefik otterrà automaticamente il certificato SSL.
