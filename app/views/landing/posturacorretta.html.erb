<% content_for :title, "Cambiare punto di vista fa la differenza" %>
<% content_for :description, "Trova la giusta posizione da cui osservare il mondo" %>

<% content_for :landing_nav do %>
  <%= render "posturacorretta/header" %>
<% end %>

<div id="top" class="bg-white text-slate-900 antialiased" data-posturacorretta-home>
  <main class="pb-16 lg:pb-24">
    
    <% @home_data.to_h.fetch("components", []).each do |component| %>
      <%= render "components/builder",
                 component: component,
                 context: { "audiences" => @audiences.to_h } %>
    <% end %>

  </main>

  <%= render "posturacorretta/footer" %>
</div>
